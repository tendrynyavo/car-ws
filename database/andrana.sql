create table categorie(
	id_categorie serial primary key,
	nom varchar(50) not null
);

create table marque(
	id_categorie serial primary key,
	nom varchar(50) not null
);

create table categorie(
	id_categorie serial primary key,
	nom varchar(50) not null
);

create table categorie(
	id_categorie serial primary key,
	nom varchar(50) not null
);